# PDMS 设备定位工具

## 1. 项目概述

本项目是一个面向 AVEVA PDMS Data Listing 文本的设备定位与几何轮廓提取工具。

工具读取 PDMS 导出的 TXT 文件，识别其中的 `EQUIPMENT`、`ZONE`、`SUBEQUIPMENT` 以及几何 Primitive，解析设备位置和方向，并一次性生成设备坐标数据、解析诊断信息、DXF 定位图和 Primitive 分析报告。

当前源码注释和说明对应 **Phase 2A**：除设备坐标解析外，已经加入 BOX/CYLINDER 的三维变换、俯视投影和凸包轮廓生成。

## 2. 当前功能

### 2.1 PDMS Data Listing 解析

- 按 `NEW <TYPE> [/名称]` 建立对象节点。
- 按 `END` 关闭对象节点。
- 使用栈维护 PDMS 对象层级关系。
- 识别 `SITE`、`ZONE`、`EQUIPMENT`、`SUBEQUIPMENT`、Primitive 和其他对象。
- 保存对象的原始属性、对象类型、名称、源文件行号、POS 和 ORI。
- 识别 `INPUT BEGIN` / `INPUT END` 区域。
- 对空行、注释行和部分 PDMS 输出辅助行进行跳过处理。
- 检查对象层级是否完整闭合，并记录残留对象和多余 `END` 等异常。

核心实现：[`PdmsDataListingParser.cs`](源码工程/Parser/PdmsDataListingParser.cs)

### 2.2 设备 POS 坐标解析

`PositionParser` 将 PDMS 的方向距离表达式转换为世界坐标，单位为 mm：

| PDMS 方向 | 坐标含义 |
|---|---|
| `E` | `+X` |
| `W` | `-X` |
| `N` | `+Y` |
| `S` | `-Y` |
| `U` | `+Z` |
| `D` | `-Z` |

例如：

```text
POS E 71700mm N 3505mm U 2021mm
```

会得到：

```text
X = 71700
Y = 3505
Z = 2021
```

解析器保留小数坐标，不主动取整，并对 POS 缺失、分量不足、格式错误和重复方向等情况返回错误信息。

核心实现：[`PositionParser.cs`](源码工程/Parser/PositionParser.cs)

### 2.3 ORI 方向解析

`OrientationParser` 将 PDMS 的 ORI 表达式转换为三维旋转变换。当前支持：

- 六个基本方向：`E`、`W`、`N`、`S`、`U`、`D`。
- 简单 ORI：`Y is E and Z is U`。
- 带角度的复合方向，例如：`S 36 W`、`E 82 U`。
- 多段复合方向，例如：`W 3.42747 N 60.5833 U`。
- 由局部 Y 轴和 Z 轴推导 X 轴，并进行正交化处理。
- 检查 Y/Z 轴是否近似平行。

旋转矩阵采用右手系，轴向量以列向量形式表达：

```text
X = Y × Z
```

核心实现：[`OrientationParser.cs`](源码工程/Parser/OrientationParser.cs)

### 2.4 三维变换链

`Transform3D` 表示局部坐标到父坐标的平移和旋转，支持：

- 单位变换。
- 纯平移。
- 方向变换。
- 父子变换复合。
- 点坐标变换。
- 旋转矩阵正交性和右手系检查。

轮廓计算使用以下递归关系：

```text
世界坐标 = 设备变换 × 父级变换 × Primitive 变换 × 局部点
```

其中，子对象的 POS 在父对象局部坐标系中解释，父级旋转会影响子对象平移方向。

核心实现：[`Transform3D.cs`](源码工程/Models/Transform3D.cs)

### 2.5 设备俯视轮廓

`OutlineBuilder` 沿以下层级递归计算 Primitive：

```text
EQUIPMENT → SUBEQUIPMENT/其他中间节点 → Primitive
```

当前已支持的几何类型：

- `BOX`
  - 使用 `XLEN`、`YLEN`、`ZLEN` 创建 8 个角点。
  - 默认认为 BOX 的 POS 位于几何中心。
- `CYLINDER`
  - 使用 `DIAM` 和 `HEIG` 创建圆柱。
  - 沿局部 Z 轴布置两端圆周。
  - 默认使用 32 个圆周采样点。

计算步骤为：

1. 读取 Primitive 的局部 POS 和 ORI。
2. 与父级变换递归复合。
3. 将三维顶点转换到世界坐标。
4. 投影到世界 XY 平面。
5. 使用 Andrew 单调链算法计算二维凸包。
6. 生成闭合俯视轮廓记录。

核心实现：

- [`OutlineBuilder.cs`](源码工程/Cad/OutlineBuilder.cs)
- [`ConvexHull.cs`](源码工程/Cad/ConvexHull.cs)
- [`Primitives.cs`](源码工程/Models/Primitives.cs)

### 2.6 解析验证与异常记录

`EquipmentValidator` 对解析结果进行汇总和检查，包括：

- EQUIPMENT 总数。
- POS 成功解析数量。
- 缺失 POS 数量。
- POS 解析失败数量。
- 缺失名称数量。
- 重复位号。
- XY 坐标完全重合的设备和分组。
- X/Y/Z 坐标范围。

所有异常通过 `ParseIssue` 保存类型、设备位号、TXT 行号、原始内容和说明，供控制台和报告使用。

核心实现：

- [`EquipmentValidator.cs`](源码工程/Validation/EquipmentValidator.cs)
- [`ParseIssue.cs`](源码工程/Models/ParseIssue.cs)

## 3. 输出功能

程序每次处理一个 TXT 文件时，按照输出目录生成以下文件：

### 3.1 JSON 设备明细

文件名：`step1_parse_result.json`

包含：

- 源文件路径。
- SITE 名称。
- 层级闭合状态。
- 解析统计。
- 每台设备的位号、ZONE、TXT 行号、原始 POS、ORI、坐标、状态和子对象类型。
- 全部解析异常。

该文件主要用于调试、追溯和后续自动处理。

JSON 输出由 [`Program.cs`](源码工程/Program.cs) 负责组织。

### 3.2 设备坐标 Excel

文件名：`equipment_coordinates.xlsx`

包含三个工作表：

1. **设备坐标**
   - 序号。
   - 设备位号。
   - X/Y/Z 坐标。
   - 原始 POS。
   - ORI。
   - 所属 ZONE。
   - TXT 行号。
   - 解析状态。
2. **解析异常**
   - 异常类型。
   - 设备位号。
   - TXT 行号。
   - 原始内容。
   - 说明。
3. **统计**
   - 设备总数。
   - 坐标解析数量。
   - 缺失或失败数量。
   - 重复和坐标重合统计。
   - 坐标范围。

实现文件：[`ExcelExporter.cs`](源码工程/Export/ExcelExporter.cs)

### 3.3 DXF 设备定位图

文件名：`equipment_location.dxf`

DXF 中使用以下图层：

| 图层 | 内容 |
|---|---|
| `EQUIP_POINT` | 设备真实定位十字，中心为设备 X/Y 坐标 |
| `EQUIP_MARKER` | 半径 200 mm 的定位圆，仅作视觉标记 |
| `EQUIP_TAG` | 设备位号文字 |
| `EQUIP_OUTLINE` | BOX/CYLINDER 的世界 XY 投影凸包 |
| `DEBUG_CENTER` | 调试模式下的 Primitive 世界原点 |
| `DEBUG_AXES` | 调试模式下的世界 X/Y/Z 轴 |
| `DEBUG_NAME` | 调试模式下的 Primitive 类型、名称和行号 |

DXF 使用 R2000 格式写出，定位十字、定位圆和设备位号按照设备原始坐标绘制，不做排版位移。

实现文件：

- [`DxfExporter.cs`](源码工程/Export/DxfExporter.cs)
- [`DxfExportResult.cs`](源码工程/Export/DxfExportResult.cs)

### 3.4 Primitive 报告 Excel

文件名：`primitive_report.xlsx`

包含三个工作表：

1. **Primitive统计**
   - 各对象类型数量。
   - 类型支持状态。
   - 类型说明。
2. **未支持Primitive**
   - 未实现几何类型的逐条记录。
   - 所属设备。
   - 父级路径。
   - Primitive 类型和名称。
   - TXT 行号。
   - 原始属性摘要。
3. **ORI解析异常**
   - 无法解析的 ORI。
   - 对象名称和类型。
   - TXT 行号。
   - 错误说明。

实现文件：[`PrimitiveReportExporter.cs`](源码工程/Export/PrimitiveReportExporter.cs)

## 4. 程序使用方式

### 4.1 命令行转换

```text
PdmsEquipmentLocator.exe convert <输入TXT> [输出目录]
```

如果不指定输出目录，默认写入程序目录下的 `output` 文件夹。

### 4.2 兼容旧版 Step1 命令

```text
PdmsEquipmentLocator.exe step1 <输入TXT> [输出目录]
```

该命令与 `convert` 进入同一套处理流程。

### 4.3 指定设备调试

```text
PdmsEquipmentLocator.exe convert <输入TXT> [输出目录] --equipment V1011A
```

调试模式用于检查单台设备，额外显示：

- 设备世界变换矩阵。
- Primitive 局部 POS/ORI。
- Primitive 世界原点。
- Primitive 世界轴方向。
- 世界三维顶点。
- 变换失败原因。

### 4.4 桌面拖放

可以将 PDMS Data Listing TXT 文件直接拖到程序 EXE 图标上。拖放模式结束时会等待用户按回车，便于查看控制台结果。

## 5. 主要源码结构

```text
源码工程/
├─ Program.cs                         程序入口、参数处理、流程编排和控制台报告
├─ PdmsEquipmentLocator.csproj        主项目配置和第三方依赖
├─ Models/                            领域模型和几何基础数据结构
│  ├─ Equipment.cs                    设备模型
│  ├─ PdmsObject.cs                   通用 PDMS 对象和原始属性
│  ├─ Position3D.cs                   三维坐标
│  ├─ Transform3D.cs                  三维平移/旋转变换
│  ├─ Primitives.cs                   BOX/CYLINDER 模型
│  ├─ ParseResult.cs                  一次解析结果
│  ├─ ParseIssue.cs                   异常记录
│  └─ ParseStatus.cs                  设备解析状态
├─ Parser/                            TXT、POS 和 ORI 解析
│  ├─ PdmsDataListingParser.cs        PDMS 层级解析
│  ├─ PositionParser.cs               POS 坐标解析
│  └─ OrientationParser.cs             ORI 方向解析
├─ Validation/                        解析结果统计和验证
│  └─ EquipmentValidator.cs
├─ Cad/                               设备轮廓和凸包计算
│  ├─ OutlineBuilder.cs               Primitive 变换和轮廓构建
│  └─ ConvexHull.cs                   二维凸包算法
├─ Export/                            JSON/Excel/DXF/Primitive 报告输出
│  ├─ ExcelExporter.cs
│  ├─ DxfExporter.cs
│  ├─ DxfExportResult.cs
│  └─ PrimitiveReportExporter.cs
└─ PdmsEquipmentLocator.Tests/        xUnit 单元测试
   ├─ PositionParserTests.cs
   ├─ OrientationParserTests.cs
   ├─ Transform3DTests.cs
   ├─ ConvexHullTests.cs
   ├─ OutlineBuilderTests.cs
   └─ PrimitiveReportTests.cs
```

## 6. 整体处理流程

```text
PDMS Data Listing TXT
        │
        ▼
PdmsDataListingParser
        │
        ▼
ParseResult
  ├─ Equipment[]
  ├─ PdmsObject 层级树
  └─ ParseIssue[]
        │
        ├─ EquipmentValidator
        │       └─ 统计和坐标/位号异常
        │
        ├─ JSON 输出
        │
        ├─ ExcelExporter
        │       └─ 设备坐标 Excel
        │
        ├─ OutlineBuilder
        │       ├─ POS/ORI 变换链
        │       ├─ BOX/CYLINDER 顶点
        │       └─ XY 凸包轮廓
        │
        ├─ DxfExporter
        │       └─ DXF 定位图和设备轮廓
        │
        └─ PrimitiveReportExporter
                └─ Primitive 统计、未支持项和 ORI 异常
```

## 7. 技术依赖

主项目当前配置：

- .NET 10：`net10.0`
- `ClosedXML 0.105.1`：Excel 文件生成
- `netDxf 2023.11.10`：DXF 文件生成

测试项目使用：

- xUnit `2.9.3`
- Microsoft.NET.Test.Sdk `17.14.1`
- coverlet.collector `6.0.4`

项目配置文件：[`PdmsEquipmentLocator.csproj`](源码工程/PdmsEquipmentLocator.csproj)

## 8. 当前支持范围与已知边界

### 已实现

- EQUIPMENT 识别和坐标提取。
- POS 的 E/W/N/S/U/D 坐标换算。
- 基本和复合 ORI 解析。
- EQUIPMENT、SUBEQUIPMENT 和 Primitive 层级递归。
- BOX/CYLINDER 三维变换和俯视凸包。
- 未支持 Primitive 的逐条报告。
- 设备坐标、异常、统计、DXF 和调试信息输出。
- 基础单元测试。

### 当前限制

- 当前只对 `BOX` 和 `CYLINDER` 生成几何轮廓。
- `CONE`、`DISH`、`PYRAMID`、`RTORUS`、`CTORUS` 等几何类型暂未生成轮廓，但会记录到未支持报告。
- ZONE 自身的 POS/ORI 目前只产生警告，没有纳入设备世界坐标变换链；含有 ZONE 偏移的文件需要人工核对。
- 设备位号文字暂未做自动避让，密集区域可能发生文字重叠。
- 几何变换和凸包内部使用 `System.Numerics.Vector2/Vector3` 的单精度浮点数；设备坐标表本身仍使用 double。
- 输入编码检测目前以 UTF-8 为主，对无 BOM 的 GBK、UTF-16 等文件需要特别确认。
- 当前测试以单元和内存夹具为主，尚未覆盖所有真实 Data Listing 变体及完整 CLI 集成场景。

## 9. 当前版本定位

本工具目前更适合用于：

- PDMS 设备坐标快速提取。
- 设备定位图初步生成。
- 设备和 Primitive 数据的人工核对。
- 后续自动化处理的中间数据生成。

如果用于正式工程交付，建议在导出后重点复核：

1. 输入文件编码。
2. ZONE 是否包含 POS/ORI。
3. 未支持 Primitive 是否影响设备外形判断。
4. 重合坐标设备是否属于设计意图。
5. DXF 中设备定位点与原始 PDMS 数据是否一致。

